model ScopeOfWorkDeliverable {
    driveId       String
    documentId    String
    id            String
    title         String
    description   String
    status        String
    githubId      Int?

    @@id([driveId, documentId, id])
}